/*
Count inversions in an array



Problem Statement: Given an array of N integers, count the inversion of the array (using merge-sort).

Inversion of an array: for all i & j < size of array, if i < j then you have to find pair (A[i],A[j]) such that A[j] < A[i].
*/

// mine brute force-> two loop if arr[j]>arr[i] inversion++; O(n2)

// sir brute force -> same as mine

// sir better O(nlogn)

// sc= O(n) temp array and say i am altering the orignal 
// array asay to interviwer if u dont want i can also make a copy instead


/*
in merge sort we know that wwe divide the array so
in every division do the logic as shown in photo that is 
if my arr 1 element is greater than all the element of arr 1 will be greater
than the right array that element
*/

#include <bits/stdc++.h>
using namespace std;

void MergeSort(vector<int>arr){
//  merge sort logic
}

int main(){

}
